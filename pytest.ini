[pytest]
markers =
    hwp: requires Hangul (HWP) COM automation - run explicitly with -m hwp
    node: requires Node.js + network (kordoc integration) - run explicitly with -m node
addopts = -m "not hwp and not node"
